中缀转后缀规则：
  从左到右编历中缀表达式的每一个数字和符号
    数字直接输出，即成为后缀表达式的一部分
    若是符号：
    若是),则将栈中元素弹出且输出，直至遇到(,(弹出栈但不输出
    若是(,*,\,+,-等符号，则从栈中弹出并输出优先级不低于其的符号，直到遇到一个优先级低的，然后将当前符号入栈
  编历结束，将栈中所有符号弹出并输出，知道栈为空
后缀表达式计算：
  数字直接进栈
  运算符，则从栈中弹出两个元素计算（后弹出的为左运算数），并将结果进栈
  编历结束，将计算结果从栈中弹出。
